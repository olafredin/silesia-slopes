# Course: Geomorphological Hazards of Slopes (University of Silesia, Katowice)
# Python 3.11+ recommended.
# Install with:    pip install -r requirements.txt

# --- Core scientific stack ---
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7

# --- Jupyter ---
jupyterlab>=4.0
notebook>=7.0
ipykernel>=6.25
ipywidgets>=8.0

# --- Geospatial libraries (added later for the LiDAR notebook) ---
rasterio>=1.3
geopandas>=0.14
rioxarray>=0.15
shapely>=2.0
